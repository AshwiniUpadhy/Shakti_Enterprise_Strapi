'use strict';

/**
 * interpretation-service controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::interpretation-service.interpretation-service');
