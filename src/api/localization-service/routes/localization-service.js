'use strict';

/**
 * localization-service router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::localization-service.localization-service');
